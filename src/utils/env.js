const webName = 'HustHostel'
const minPrice = 0,
  defaultPriceRight = 4500000,
  minArea = 10,
  defaultAreaRight = 200,
  maxPrice = 700000000,
  maxArea = 20005
export default webName
export { minPrice, minArea, defaultAreaRight, defaultPriceRight, maxPrice, maxArea }
